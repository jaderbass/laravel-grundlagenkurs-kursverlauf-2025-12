Hier können Teilnehmende eigene Notizen ablegen.

# eigene Notizen

## Commits und Tags in Git setzen

```bash
git add .
git commit -m "release: v1.3.0 – Kursmaterial aktualisiert"
git tag v1.3.0
git push
git push origin v1.3.0
```